//: Playground - noun: a place where people can play

import UIKit

// Optional to add type. This one does not have it.
var str = "Hello, playground"

// This does have the type specified.
var str2:String = "Hello, playground"

// Since swift infers types, you can use type specifiers to create
// unitialized variables of specified type.
var str3:String

// nil means nothing
// Null means its not there

// Strings can not be nil by default, however if you add the "?" it becomes optional
// and then you can have a nil string variable.
// ie. this does not work
// var str4:String = nil
// but this does
var str4:String? = nil

// variable
var x = 1

// const
let y = 1

// Hold down option/alt key and click on a varible or class name to pull up help
// Hold down command key and click to find source a type such as String
// Remove comment on the string
// String

// Drag a file name to left side tool bar. Click the left panel navigation
// tab if its not there.  On right side, click the eyeball to see.  Click
// the circle to embed inline to playground.
var image = UIImage(named: "skinkdesktop.jpg")

// open up the right panel "utilities panel" then click the quick help tab
// Then click on a variable and it will open help for the item clicked.

// How to print a variable
print(str)
